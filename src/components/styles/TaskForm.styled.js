import styled from "styled-components";

export const TaskFormStyled = styled.form`
  padding: 1.5rem;
`;

export const TaskFormHeader = styled.h3`
  text-transform: uppercase;
  font-size: 1.7rem;
  font-weight: 600;
  letter-spacing: 2px;
  padding-bottom: 1rem;
  margin-bottom: 2rem;
  border-bottom: 1px solid var(--color-secondary);
  width: max-content;
`;
